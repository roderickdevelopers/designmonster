<span class="list-item"  {{action 'toggleDone'}}>{{item.content}}</span><button class="btn btn-xs btn-danger" {{action 'delete'}}>DELETE!</button>

{{input class="list-item" type="checkbox" checked=item.done change=(action "toggleDone")}}
